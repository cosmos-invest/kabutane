# かぶたね 開発上の不変ルール

このリポジトリを変更するAI・開発者は、以下を製品仕様として扱うこと。会話や実装方針が変わっても、ユーザーから明示的な変更指示がない限り維持する。

## 1. 暫定GCの「発見」はプレミアム専用

- 進行中月で `provisional_status === "GC"` となった銘柄を**一覧から発見する機能**は、プレミアム専用の先回り情報として扱う。
- 公開の全銘柄一覧、公開フィルター、公開サマリー、公開ランキング、初心者向けキャラクターフィルター、公開FAQの導線で「暫定GC銘柄の一覧」を特定できる形にしない。
- 公開の全銘柄一覧では暫定GCを専用フィルターにしない。公開一覧上は前月末の確定状態を優先し、暫定GCを示すラベルや進行中月のRSIクロス値を出さない。
- 暫定GCの全銘柄スクリーニング、優先順位、需給との複合判定、GC系×需給改善などの発見機能は `premium-supply-beta.html` 側の価値とする。
- **個別銘柄ページは例外。** ユーザーが銘柄コードや銘柄名を指定してその銘柄を見に来た場合は、進行中月の暫定GC / 暫定DC、月足RSI、5か月MA、差分を表示してよい。個別ページは「既知の銘柄を確認する場所」であり、「暫定GC銘柄を発見する一覧」ではない。
- **手動ウォッチリストも個別確認と同じ例外。** ユーザー自身が銘柄を `☆ 気になる` で登録したブラウザ内ウォッチリストでは、その登録済み銘柄だけ暫定GC / 暫定DCを表示してよい。ウォッチリストを暫定GC条件で自動追加・全市場抽出・順位付けしてはならない。
- したがってプレミアムの価値は「暫定GCという事実を秘密にすること」ではなく、**全対象銘柄から暫定GCを発見・抽出・順位付けし、需給や出来高と組み合わせて観察候補を提示すること**に置く。
- 公開の全銘柄一覧は `data/core/public-radar.json` のような一覧用の公開データを使い、暫定GCを一覧化しない。個別詳細用データでは暫定GCを保持してよい。
- 現在のGitHub PagesとGitHubリポジトリ自体は認証付き会員サイトではなく「URLを購入者へ限定共有するβ運用」。本格課金時に厳密な会員限定データへ移す場合は、認証付き配信とデータAPIの会員境界を別途設計する。

## 2. 公開の全銘柄一覧は初心者を迷わせない

- 対象はプライム・スタンダード・グロースの内国普通株。ETF / ETN / REIT / TOKYO PRO / 外国株等を混ぜない。
- 全件を見られることは維持しつつ、初心者向けには **コスモス🌸 / ルーモ✨ / エール💜 の3つの案内フィルター**を先に見せる。
- 自由な複合条件は「玄人向け設定」に畳み、初見の画面へ大量の条件を露出させない。

### コスモス🌸
王道の上昇トレンドと勢いの両立を見る。長期線より上、移動平均の並び、52週高値との距離を重視する。

### ルーモ✨
チャートの「動き出し」を見る。直近出来高の増加、短中期線より上、高値への接近を重視する。

### エール💜
無理をしにくい会社を慎重に見る。財務取得済みを前提に、自己資本、ROE、FCF、売上成長などの基礎体力を重視する。

## 3. ウォッチリストはブラウザローカルを基本とする

- `気になる株` はログイン不要で各ブラウザの `localStorage` に保存する。別端末・別ブラウザへ自動同期しない。
- 保存対象は銘柄コード・表示名・追加日時などの最小限とし、サーバーへ個人ウォッチリストを送信しない。
- 保存可能な銘柄コードは、かぶたねが扱うJPXの4文字コード形式へ制限し、URL queryやlocalStorage由来の文字列を未エスケープのままHTMLへ挿入しない。
- ブラウザデータ削除やシークレットモードでは消える可能性を画面上で説明する。
- ウォッチリストは「既知銘柄を追う場所」であり、プレミアムの全市場発見機能の代替にしない。

## 4. プレミアム観察優先度は必ず検証可能にする

- 観察優先度を生成した当日のスコア・構成要素・順位・株価を**その時点の値として履歴保存**し、未来の結果が出た後に過去スコアを書き換えない。同じ株価基準日でも内容が変わった再生成は別generationとして残し、完全同一内容だけをidempotentとして扱う。
- 各銘柄の `price_date` とsnapshot基準日が一致するものだけをその日のcohortへ採用し、古い価格を新しい日のエントリー価格として使わない。
- 仮想ポートフォリオは上位N銘柄や点数閾値を固定ルールで組み、5営業日後・20営業日後の騰落率を追跡する。
- 成熟した5営業日後・20営業日後の結果は `data/premium/research/outcomes/<engine_version>/` の別台帳へ固定し、後日の1年チャート窓更新やユニバース変更で過去成績を再計算・消失させない。
- `auto_adjust=False` のpoint-in-time価格で期間リターンを測る場合、cohort日の翌日から実際のexit日までに発生した株式分割・併合比率を累積して経済的リターンへ補正してからoutcome台帳へ固定する。補正係数も保存し、分割・併合を投資損益と誤認しない。
- ターゲット日に現行価格shardから銘柄が消えている場合は、point-in-time履歴に残る最後の観測価格を使用したことを `last_observed_before_target` と明記し、単純にポートフォリオ母集団から除外しない。
- 成績は市場全体の同期間平均と比較し、単純な上昇相場をエンジンの実力と誤認しない。成熟cohortは原則として構成銘柄の95%以上のoutcomeが確定してから集計する。
- 重みの最適化は現行エンジンとは別の challenger として並走させる。検証結果だけで本番重みを自動変更しない。
- 過学習を避けるため、正式な重み見直し候補は週次コホートで判定し、少なくとも5営業日後の成熟コホート20週、20営業日後の成熟コホート12週がたまるまでは現行エンジンを維持する。
- スコア仕様を変更するときは `engine_version` を更新し、履歴とoutcome台帳をバージョン別ディレクトリへ分離して旧バージョンと混同しない。

## 5. 説明ページ同期

機能や判定ルールを変えたら `learn.html` も同時に更新する。表示だけ変更して説明を古いまま残さない。
