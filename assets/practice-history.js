(function(){"use strict";if(typeof document==="undefined"||typeof PracticeHistoryCore==="undefined")return;let lastSignature="";function finite(v){const n=Number(v);return Number.isFinite(n)?n:null}function build(){if(typeof state==="undefined"||!state.rows?.length)return null;const row=typeof currentRow==="function"?currentRow():state.rows[state.cursor];const metrics=typeof ReplayPro!=="undefined"?ReplayPro.accountMetrics(state.account,row?.close,state.initialCapital):{};const guide=state.guided||{};const checks=guide.checks||{};const achieved=Object.values(checks).filter(Boolean).length,total=Object.keys(checks).length;const buys=(state.trades||[]).filter(t=>String(t.type).includes("BUY")).length,sells=(state.trades||[]).filter(t=>String(t.type).includes("SELL")).length;const start=state.rows[state.startIndex]?.date||state.rows[0]?.date||"";const end=row?.date||"";const signature=`${state.code}-${start}-${end}-${buys}-${sells}-${metrics.totalProfit}`;return{id:signature,savedAt:new Date().toISOString(),code:String(state.code||state.payload?.code||""),name:state.payload?.name||state.code||"",startDate:start,endDate:end,mode:guide.mode||"free",outcome:guide.outcome||"manual",initialCapital:finite(state.initialCapital),totalReturn:finite(metrics.totalReturn),totalProfit:finite(metrics.totalProfit),maxDrawdown:finite(state.maxDrawdown),buyCount:buys,sellCount:sells,ruleAchieved:achieved,ruleTotal:total,trades:(state.trades||[]).slice(-60).map(t=>({date:t.date,type:t.type,price:t.price,shares:t.shares,realized:t.realized,memo:t.memo||t.reason||""}))}}function save(){const entry=build();if(!entry||entry.id===lastSignature)return;lastSignature=entry.id;PracticeHistoryCore.add(entry);const status=document.querySelector("[data-share-status]");if(status&&!status.textContent)status.textContent="この練習結果を端末の履歴へ保存したよ。"}function observe(){const target=document.getElementById("finishSummary");if(target)new MutationObserver(()=>{if(!target.hidden||state?.ended||state?.guided?.step==="finished")setTimeout(save,0)}).observe(target,{attributes:true,childList:true,subtree:true});document.addEventListener("kabutane:open-share-report",save);document.addEventListener("click",e=>{if(e.target.closest('[data-guided-action="finish"],#finishButton'))setTimeout(save,120)})}if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",observe);else observe();window.KabutanePracticeHistory={saveCurrent:save}})();