(function(){"use strict";const $=s=>document.querySelector(s);const finite=v=>{const n=Number(v);return Number.isFinite(n)?n:null};const money=v=>{const n=finite(v);return n===null?"—":`${Math.round(n).toLocaleString("ja-JP")}円`};const pct=v=>{const n=finite(v);return n===null?"—":`${n>0?"+":""}${n.toLocaleString("ja-JP",{maximumFractionDigits:2})}%`};const esc=v=>String(v??"").replace(/[&<>'"]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c]));function render(){const rows=PracticeHistoryCore.read(),s=PracticeHistoryCore.summary(rows);$("#historySummary").innerHTML=`<article><span>練習回数</span><strong>${s.total}回</strong></article><article><span>利益が出た練習</span><strong>${s.profits}回</strong></article><article><span>損切りを実行</span><strong>${s.stops}回</strong></article><article><span>ルール達成率</span><strong>${s.ruleRate===null?"—":`${s.ruleRate.toFixed(0)}%`}</strong></article>`;$("#historyList").innerHTML=rows.map(r=>`<article class="history-card"><div class="history-card-head"><div><h3>${esc(r.name)} <small>(${esc(r.code)})</small></h3><small>${esc(r.startDate)}〜${esc(r.endDate)}｜${r.mode==="guided"?"はじめてモード":"自由練習"}</small></div><button type="button" class="button ghost-button" data-delete-history="${esc(r.id)}">削除</button></div><div class="history-metrics"><div><span>総損益率</span><strong class="${finite(r.totalReturn)>=0?"positive":"negative"}">${pct(r.totalReturn)}</strong></div><div><span>総損益</span><strong>${money(r.totalProfit)}</strong></div><div><span>最大DD</span><strong>${pct(r.maxDrawdown)}</strong></div><div><span>ルール達成</span><strong>${r.ruleTotal?`${r.ruleAchieved}/${r.ruleTotal}`:"—"}</strong></div></div><p class="mini-dialogue">買い ${r.buyCount||0}回 / 売り ${r.sellCount||0}回｜終了理由 ${esc(r.outcome||"—")}</p></article>`).join("")||'<div class="history-empty"><strong>まだ練習履歴がないよ</strong><p>売買練習を最後まで終えると、この端末へ自動保存されます。</p><a class="button" href="replay.html">はじめてモードで練習する</a></div>'}document.addEventListener("click",e=>{const b=e.target.closest("[data-delete-history]");if(!b)return;PracticeHistoryCore.remove(b.dataset.deleteHistory);render()});$("#historyExport").addEventListener("click",()=>{const blob=new Blob([PracticeHistoryCore.exportText(PracticeHistoryCore.read())],{type:"application/json"});const url=URL.createObjectURL(blob),a=document.createElement("a");a.href=url;a.download=`kabutane-practice-history-${new Date().toISOString().slice(0,10)}.json`;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000)});$("#historyImport").addEventListener("change",async e=>{const file=e.target.files?.[0];if(!file)return;try{PracticeHistoryCore.importText(await file.text());render()}catch(err){alert(err.message||"読み込めませんでした。")}});$("#historyClear").addEventListener("click",()=>{if(confirm("この端末の練習履歴をすべて削除しますか？")){PracticeHistoryCore.clear();render()}});render()})();