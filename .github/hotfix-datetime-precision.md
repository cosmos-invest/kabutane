# Datetime precision hotfix trigger

This temporary marker triggers the guarded workflow that applies and verifies the chart-event datetime precision fix.

Retry: exact block replacement with source validation before tests.